= form_for([game, user]) do |f|
  - if user.errors.any?
    %div#error_explanation
      %h2
        = pluralize(user.errors.count, "error")
        prohibited this user from being saved:</h2>

      %ul
      - user.errors.full_messages.each do |message|
        %li= message

  %div.form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'

  = f.submit
