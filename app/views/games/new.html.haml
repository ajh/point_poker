%h1 New game

= form_for(@game) do |f|
  - if @game.errors.any?
    %div#error_explanation
      %h2
        = pluralize(@game.errors.count, "error")
        prohibited this game from being saved:</h2>

      %ul
      - @game.errors.full_messages.each do |message|
        %li= message

  %div.form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'

  = f.submit
