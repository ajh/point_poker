%p#notice
  = notice

%p
  %strong Name:
  = @game.name

%h1 Rounds

%ul
  - @game.rounds.each do |round|
    %li= render round

%h1 Create Round

= render partial: 'rounds/form', locals: {game: @game, round: @game.rounds.build}

